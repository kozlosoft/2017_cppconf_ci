dist: trusty
language: cpp

script:
  - mkdir build && cd build
  - cmake .. -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DCMAKE_CXX_COMPILER=$CMAKE_CXX_COMPILER
  - cmake --build .
  - ./hello

matrix:
  include:
    - addons:
        apt:
          sources:
          - ubuntu-toolchain-r-test
          packages:
          - g++-4.9
      env:
        - CMAKE_CXX_COMPILER=g++-4.9
        - BUILD_TYPE=Release COMPILER=g++-4.9
        - BUILD_TYPE=Debug COMPILER=g++-4.9
    - addons:
        apt:
          sources:
          - ubuntu-toolchain-r-test
          packages:
          - g++-5
      env:
        - CMAKE_CXX_COMPILER=g++-5
        - BUILD_TYPE=Release COMPILER=g++-5
        - BUILD_TYPE=Debug COMPILER=g++-5
    - addons:
        apt:
          sources:
          - ubuntu-toolchain-r-test
          packages:
          - g++-6
      env:
        - CMAKE_CXX_COMPILER=g++-6
        - BUILD_TYPE=Release COMPILER=g++-6
        - BUILD_TYPE=Debug COMPILER=g++-6
    - addons:
        apt:
          sources:
          - ubuntu-toolchain-r-test
          - llvm-toolchain-trusty-3.9
          packages:
          - clang-3.9
          - g++-6
      env:
        - CMAKE_CXX_COMPILER=clang++-3.9
        - BUILD_TYPE=Release COMPILER=clang++-3.9
        - BUILD_TYPE=Debug COMPILER=clang++-3.9